- Prefers sizing hero-section particle text to fill the hero's visual space: uses large, responsive `clamp()`-based font sizes and a section-filling container height (e.g. `h-[80vh]`) rather than a small, fixed-size text block, so the animation occupies the hero section. Confidence: 0.5# User Taste Profile
- Prefers selective adoption of redesign elements: keep the good parts of a redesign, revert the rest to the original design rather than wholesale replacement. Confidence: 0.95
- Prefers a pure white light background with ink-black dark theme using semantic CSS class names (e.g. `bg-paper text-ink dark:bg-ink dark:text-paper`), i.e. the craft.wild.as palette, applied consistently across all UI elements (body, header/masthead, borders, text) — over warm cream, Tailwind gray defaults, or hardcoded hex values. Confidence: 0.75
- Prefers using semantic CSS class names for color variables (e.g. `bg-paper`, `text-ink`, `bg-ink`, `text-paper`) defined in the CSS, rather than hardcoding hex values directly in Tailwind class attributes. Confidence: 0.7
- Prefers the font pairing of Onest (clean sans-serif for display/headings/body) + Geist Mono (monospace for labels and technical UI). Confidence: 0.85
- Prefers masthead-style headers: sticky/overlay header with backdrop blur, navigation as uppercase monospace labels, and NO centered logo. Confidence: 0.8
- Prefers monospace uppercase "eyebrow" labels for section headers and UI text (small font size, wide tracking). Confidence: 0.8
- Likes subtle line-by-line text reveal animations for hero headlines (staggered, with a small initial delay). Confidence: 0.4
- Prefers particle-based canvas text animations for hero headlines over simple line-by-line reveal effects — favors effects with configurable scatter, gather duration, stagger, pointer repulsion, idle drift, and reduce-motion fallback (e.g. reactbits.dev ParticleText). Confidence: 0.5
- Prefers thin subtle border dividers (e.g. `border-black/8` or with low opacity) and divide separators for content sections. Confidence: 0.6
- Prefers a borderless header/masthead — explicitly asks to remove the bottom border that separates the sticky header from page content below; subtle divider borders are still acceptable between content sections. Confidence: 0.8
- Prefers a "Point of View" / philosophy section on the homepage near the top, with a short personal mission or blog philosophy statement. Confidence: 0.75
- Prefers the hero headline text "# Justart-dev\n개발 기록" (line-broken, with a code-style "#" tag prefix, positioned slightly higher than center via negative top margin) on the landing page. Confidence: 0.7
- Prefers single-line, tag-chained hero headline text — stringing multiple tags/phrases together horizontally (using delimiters like `#`, `|`, `/`, `::`, `→`, `—`) rather than `\n` line breaks, since center-reveal scramble animations read better on one line. Explicitly found the previous line-broken "# Justart-dev\n개발 기록" too bland (밋밋하다) and moved to this tag-style format. Confidence: 0.8
- Prefers a full-viewport hero section (`min-height: 100vh`) with centered content, where the POINT OF VIEW section appears below and only becomes visible on scroll — the two sections should be split across separate "screens" rather than sharing viewport space. Confidence: 0.75
- Runs typecheck (`tsc --noEmit`) and lint as part of every change verification cycle. Confidence: 0.6
- Prefers using negative top margins (e.g. `mt-[-Nrem]`) to push landing page hero/content sections above vertical center, and is willing to iterate on the margin value (e.g. `mt-[-4rem]` → `mt-[-6rem]`) to fine-tune exact vertical placement. Confidence: 0.7
- Verifies changes across build, typecheck, lint, and dev mode (all must pass). Confidence: 0.6
- Preserves existing content structure/depth when tidying presentation: prefers hiding/collapsing excess content (e.g. collapsible TOC sections) over cutting or restructuring the underlying headings/content. Confidence: 0.8
- Prefers clean MDX files: dislikes inline HTML/JSX elements embedded directly in MDX content that make the source messy and hard to read. Prefers keeping MDX semantic and moving presentation logic into components or CSS instead. Confidence: 0.75
- Prefers applying the Eyebrow component styling consistently across ALL section labels (using the POINT OF VIEW section as the template pattern), rather than section-specific inline label divs with hardcoded classes. Confidence: 0.8
- Prefers matching hero headline text color to the Eyebrow/section label color (e.g. `text-muted`) for visual consistency across components, rather than using the default primary ink/paper text colors for the hero. Confidence: 0.7
- Prefers minimal section headers with a single Eyebrow label per section — avoids redundant descriptive subheadings (e.g. h3 elements) beneath the label. Confidence: 0.8
- Prefers concise descriptions and content, removing redundant qualifiers that add little meaning or narrow scope unnecessarily (e.g. removing "frontend" from a broader "web development" topic list). Confidence: 0.7
- Prefers English UI labels and navigation text over Korean (e.g. "View all" instead of "전체보기", "Recent writing" instead of "최근 글") — applies to interactive/UI text, not hero headlines or content body which may remain Korean. Confidence: 0.85
- Prefers English text in animated/scramble text elements (e.g. DecryptedText, canvas-based animations) because Korean characters have variable widths that cause height instability and visual jitter during animation, whereas consistent Latin character widths minimize height variation. This applies even to hero headline text that would otherwise remain Korean. Confidence: 0.85
- Prefers generous vertical spacing (padding/margins) on content sections — likes ample top/bottom breathing room around section blocks (e.g. expanding `py-12` to `py-16 md:py-20`, increasing `mt-4` to `mt-6`). Confidence: 0.7
- References external component libraries (e.g. reactbits.dev) as templates for specific animation effects rather than building from scratch — actively ports reference implementations into the codebase. Confidence: 0.85
- Prefers resolving font families dynamically for canvas-based text rendering via the `fontFamily="inherit"` pattern (using `getComputedStyle` to resolve the actual font from the DOM) rather than hardcoding font names or passing CSS variables that canvas cannot interpret. Confidence: 0.7
- Prefers integrating component CSS into Tailwind's `@layer components` directive rather than maintaining separate `.css` files imported alongside components. Confidence: 0.6
- Prefers communicating in Korean (native-level, casual tone) rather than English. Confidence: 0.8
- Expects the agent to handle the complete deployment pipeline end-to-end without step-by-step confirmation: commit changes, merge to main, push to remote, build, and deploy (e.g. to Cloudflare Pages via Wrangler) — delegates the full release flow proactively when development work is done. Confidence: 0.9
- Prefers theme-aware color resolution for canvas-based ParticleText components: passes CSS custom property references (e.g. `var(--particle-color)`, `var(--particle-highlight)`) as color props and resolves them at runtime via `getComputedStyle` so the canvas text adapts to the site's dark/light theme, rather than hardcoding hex colors. Confidence: 0.75
- Prefers sizing hero-section particle text to fill the hero's visual space: uses large, responsive `clamp()`-based font sizes and a section-filling container height (e.g. `h-[80vh]`) rather than a small, fixed-size text block, so the animation occupies the hero section. Confidence: 0.8
- Prefers brief, casual, iterative communication — uses filler words like "일단" ("for now") and gives concise directives, trusting the agent to figure out implementation details. Confidence: 0.7
- Prefers CSS-based character scramble text animations (e.g. React Bits' DecryptedText) over canvas-based particle systems for hero headlines — uses theme-aware Tailwind CSS classes (text-ink, text-muted, etc.) for revealed/encrypted character colors rather than canvas variables. Confidence: 0.6
- Prefers scroll-into-view triggered (animateOn='view') text animations that run once per page load, rather than hover-triggered, for hero headline text effects. Confidence: 0.5
- Prefers vivid, metaphorical, or developer-slang style Korean hero headline text over plain/literal descriptions — explicitly found "# Justart-dev\n개발 기록" too bland (밋밋하다) and asked for more unique, developer-centric phrasing (e.g. dev slang, philosophical metaphors, developer humor). Confidence: 0.55
- Prefers sequential character-by-character reveal (sequential={true}) with center-origin direction (revealDirection='center') for text scramble animations on hero headlines. Confidence: 0.45
- Prefers strictly isolating text-animation layout through CSS (a clamped fixed `height` + `overflow: hidden` on the animation container) rather than modifying the text-shuffle logic itself — explicitly reverted the prior `\n`-preservation fix in `shuffleText` and rejected even `min-height` in favor of a fixed-height container that clips overflow, so that the label above stays in its original position and downstream content is unaffected even when the animation's height varies wildly. Confidence: 0.85
  on) above the fold. Confidence: 0.75
- Prefers a subtle scroll-down indicator at the bottom of hero/first sections to signal scrollability: uses `@heroicons/react` (e.g. `ChevronDownIcon`), applies Tailwind's built-in `animate-bounce`, positions it centered via `absolute bottom-20 left-1/2 -translate-x-1/2` (with `relative` on the parent section for positioning context), and styles it with a muted/translucent color (`text-muted/60`) so it remains unobtrusive and doesn't clutter the UI. Prefers it positioned in the middle-to-lower area of the hero section (not the very bottom edge) and fine-tunes the `bottom-*` value (e.g. `bottom-16` to move up, `bottom-24`/`bottom-28` to move down). Confidence: 0.85
- Prefers minimal UI — removes secondary navigation/action buttons (e.g. "View all") in favor of a cleaner, less-cluttered interface without redundant link chrome. Confidence: 0.7
- Prefers theme toggle and language switcher controls present in the header — expects them to persist through design iterations rather than being dropped during redesigns. Confidence: 0.7
- Prefers click-triggered search functionality accessible from the header — expects it to persist through design iterations rather than being dropped during redesigns. Confidence: 0.7
- Prefers text color-only changes on hover in dropdown menus (theme/language selectors) over background color changes — background color shifts that obscure option text reduce readability; text-color transitions (e.g. text-muted to text-ink/dark:text-paper) with a fixed semantic background (bg-paper/dark:bg-ink) keep options legible. Confidence: 0.8
