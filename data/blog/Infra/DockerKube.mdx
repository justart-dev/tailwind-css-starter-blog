---
title: '도커에서 쿠버네티스까지 실습으로 흐름 정리'
date: '2026-05-17'
tags: ['Infra']
draft: false
summary: 'Dockerfile로 이미지를 만들고 Docker Hub에 공유한 뒤, Kubernetes에서 컨테이너로 실행되기까지의 흐름을 단계별로 정리'
---

## 목차

1. 들어가며
2. 컨테이너
3. 실습: 도커 컨테이너 빌드와 배포
4. 쿠버네티스: 핵심 개념 정리
5. 실습: 쿠버네티스 객체 배포와 검증
6. 정리

---

## 1. 들어가며

도커와 쿠버네티스의 개념적인 부분은 이전에 공부했지만, 실제로 구축해보고 사용해본 경험은 없었다. 실습까지 해보고자 했지만, 여러 사정으로 한동안 공부에 손을 놓게 되면서 결국 해보지 못했다. (반성합니다...)

그러다 `EKS`(Amazon Elastic Kubernetes Service)와 `Helm`을 사용하게 되면서, 잊고 있던 이 부분을 다시 익혀야겠다는 생각이 들었다. 그래서 **그림으로 이해하는 도커와 쿠버네티스** 라는 귀여운 표지의 책을 읽으며 복습하고, 정리해보기로 했다.

깊은 내용은 이전 글을 살펴보자. 이번에는 실습과 함께 작성하겠다.

실습 환경은 [Docker Desktop for Mac](https://docs.docker.com/desktop/setup/install/mac-install/)을 기준으로 준비했고, Rosetta가 필요한 경우 아래 명령어로 설치하면 된다.

```bash
softwareupdate --install-rosetta
```

## 2. 컨테이너

컨테이너의 기본적인 작업 흐름은 아래와 같다.

**이미지 작성(Build) -> 이미지 배포(Ship) -> 컨테이너 실행(Run)**

이미지 설계를 기록한 파일을 입력하면 빌드 결과로 이미지를 얻을 수 있다. 이미지를 저장하고 배포하는 레지스트리라는 서버를 사용한다.

이미지를 레지스트리에 업로드하고 서비스 환경과 테스트환경에서는 레지스트리 서비스에서 이미지를 가져와서 컨테이너를 실행한다.

- 도커는 **하나의 머신**에서 컨테이너 관리, 이미지 작성, 이미지 공유 등 컨테이너에 관련된 기본적인 작업 흐름을 지원하는 도구.

- 쿠버네티스는 **여러 대의 머신**으로 구성된 환경에서 컨테이너를 관리하는데 사용하는 오케스트레이션 엔진.

## 3. 실습: 도커 컨테이너 빌드와 배포

이번 실습에서는 먼저 로컬에서 Build/Run을 확인한 뒤, Ship을 다룬다.

### 1) Build / Run

도커를 사용해보기 위해 가장 단순한 예제로 "Hello, Docker!" 를 출력하는 셸 스크립트를 컨테이너에서 실행해보자.

hello.sh에는 아래 내용을 작성한다.

```bash
#!/bin/sh

echo "Hello, Docker!"
exec sleep infinity # 서버 계속 떠있게 하기 위한 임시
```

Dockerfile에는 아래 내용을 작성한다.

```bash
FROM alpine:3.23 # 베이스 이미지 지정

WORKDIR /app # 컨테이너 내부 작업 디렉터리 설정 (이후 명령어들은 기본적으로 /app 디렉토리에서 실행됨)

COPY hello.sh /app/hello.sh # 로컬 파일을 컨테이너 내부로 복사

RUN chmod +x hello.sh # 빌드 시점에 실행 권한 부여

CMD ["./hello.sh"] # 컨테이너 실행 시 기본으로 실행할 명령
```

이제 이미지를 빌드하고 컨테이너를 실행한다.

```bash
docker build -t hello-docker:v1 ./myimage # ./myimage를 빌드 컨텍스트로 사용해 hello-docker:v1 이미지 생성
docker image ls hello-docker:v1 # 생성된 이미지가 로컬에 있는지 확인
docker run --name hello-container hello-docker:v1 # 실행한 컨테이너를 중지하거나 삭제할 때 지정하기 편함
```

실행 결과는 아래와 같다. (Docker Desktop Terminal)

<Image
  src="/static/images/docker-desktop.png"
  alt="Docker Desktop에서 실행 결과 확인"
  className="mx-auto"
  width="900"
  height="600"
/>

컨테이너 정지 및 삭제는 아래의 명령어를 사용한다.

```bash
docker stop hello-container # 실행 중인 컨테이너 정지
docker rm hello-container # 정지된 컨테이너 삭제
```

### 2) Ship

도커는 작성한 이미지를 다른 호스트에 배포할 수 있다. 이미지는 **레지스트리** 라고 부르는 이미지 배포용 서버를 통해서 다른 호스트와 공유한다.

레지스트리가 도커 허브가 아니라면 이미지 이름에 레지스트리명도 포함되어야 한다. 서비스마다 조금 다르니 꼭 확인하자. 우리는 도커 허브를 이용한다.

도커 허브에 이미지를 올리려면 이미지 이름이 `계정명/이미지명:태그` 형태여야 한다. (Docker ID는 변경 불가해서 새로 파야한다고 한다.)

```bash
docker login # [Username: OOO] 이렇게 나옴!
```

이미 `hello-docker:v1`이라는 이름으로 이미지를 만들어두었다면, `docker tag`로 도커 허브에 올릴 이름을 하나 더 붙일 수 있다.

```bash
docker tag hello-docker:v1 parklego/hello-docker:v1 # 기존 이미지에 새 이름 부여
docker push parklego/hello-docker:v1 # 도커 허브에 이미지 업로드
```

반대로 처음부터 도커 허브에 올릴 이름으로 빌드할 수도 있다. 기존 로컬에 있는 이미지 지우고 바로 빌드를 때려보자.

```bash
docker build -t parklego/hello-docker:v1 ./myimage # 빌드하면서 이미지 이름 지정
docker push parklego/hello-docker:v1 # 도커 허브에 이미지 업로드
```

이렇게 업로드한 이미지는 다른 환경에서 `docker pull`로 가져올 수 있다. 가져온 이미지는 로컬 파일이 아니라 Docker 이미지로 등록되며, 이후 `docker run`으로 실행할 수 있다.

```bash
docker pull parklego/hello-docker:v1 # 도커 허브에서 이미지 내려받기
docker image ls parklego/hello-docker:v1 # 내려받은 이미지가 있는지 확인
docker run --name hello-container parklego/hello-docker:v1 # 내려받은 이미지로 컨테이너 실행
```

도커에는 볼륨, 네트워크, Docker Compose, 멀티 스테이지 빌드 등 더 많은 개념이 있다. 다만 이번 글의 목적은 쿠버네티스로 넘어가기 전에 이미지가 만들어지고, 실행되고, 레지스트리에 공유되는 흐름을 이해하는 것이므로 여기까지만 정리한다.

## 4. 쿠버네티스: 핵심 개념 정리

쿠버네티스의 주요 특징 중 하나가 **선언적**으로 어플리케이션을 관리할 수 있다는 점이다.

어플리케이션과 어플리케이션을 구성하는 컨테이너는 어떤 상태가 이상적인 상태인지 YAML 또는 JSON 형식의 매니페스트 설정 파일로 선언한다. 그러면 지정한 상태를 실현하고 유지하는 구체적인 작업을 쿠버네티스가 알아서 처리해준다.

컨테이너 그룹을 실행하는 머신 집합을 `클러스터(cluster)`라고 하고, 각 컨테이너가 실행되는 머신을 `노드(node)`라고 한다.

쿠버네티스 클러스터 전체의 관리를 담당하는 컴포넌트를 `컨트롤 플레인(control plane)`이라고 한다. 정보 조회와 변경 요청을 HTTP API를 사용해서 클러스터를 조작하지만 직접 API를 호출한다면 꽤 번거롭다. 그래서 쉽게 명령어로 정리한 `kubectl` 명령어를 사용하면 편리하다. (kubernetes + control)

### Pod

도커는 컨테이너 1개가 기본 단위이지만, 쿠버네티스에서 가장 기본적인 배포 단위는 하나 이상의 관련된 컨테이너 그룹을 묶은 `파드(pod)`다.

쿠버네티스는 IP주소를 파드마다 할당하므로 파드끼리 서로 다른 IP 주소를 사용해서 통신할 수 있다. 하나의 파드 안에 있는 컨테이너들은 네트워크 주소를 공유한다. 그래서 컨테이너끼리 통신할 때는 복잡한 IP 대신 **localhost**를 사용해 서로 통신할 수 있다.

보통은 **1 파드 = 1 컨테이너**로 사용하지만, 로그 수집기 등이 필요할 때 여러 컨테이너를 하나의 파드로 묶어서 배포하기도 한다.

### Namespace

쿠버네티스 클러스터 하나를 여러 개의 논리적인 단위로 나누어 사용하는 것을 `네임스페이스(Namespace)`라고 한다. 실무에서 포트 포워딩 시 `-n` 옵션을 사용하는 이유도 해당 파드가 속한 논리적 구역을 명시하기 위함이다.

네임스페이스는 다음과 같은 역할을 한다.

- **논리적 격리**: 하나의 클러스터 내에서 개발(dev), 운영(prod) 등 용도에 따라 리소스를 분리하여 관리할 수 있다.
- **이름 범위(Scope)**: 파드 이름은 네임스페이스 내에서만 유일하면 된다. 즉, 서로 다른 네임스페이스라면 동일한 이름의 파드가 존재할 수 있다.
- **리소스 제한**: 특정 네임스페이스가 사용할 수 있는 CPU나 메모리 자원의 총합을 제한할 수 있다.

포트 포워딩 명령을 실행할 때 특정 네임스페이스를 지정하지 않으면 기본적으로 `default` 네임스페이스에서 파드를 찾는다. 파드가 다른 구역에 생성되어 있다면 찾을 수 없다는 에러가 발생하므로 반드시 `-n` 옵션으로 정확한 위치를 알려주어야 한다.

### Deployment

노드에서 장애가 발생하거다 파드 동작 불량으로 클러스터 전체의 파드 가동 개수가 지정한 숫자보다 줄어들면 자동적으로 새로운 파드를 실행해서 복구하는 **셀프 힐링** 기능을 지원한다.

일정 개수의 파드가 클러스터에서 언제나 가동 중인 상태를 유지하면서 파드 그룹을 점진적으로 업데이트 또는 롤백하는 **롤링 업데이트** 방식도 지원한다.

<div className="my-8 border-l-4 border-primary-500 pl-6">
  <h3 className="mt-0 text-xl font-bold">Context 관리</h3>

`kubectl` 명령어는 클러스터 내부가 아니라 내 로컬 PC에서 실행하는 명령어이다. 실무에서는 로컬, 개발, 운영 등 여러 개의 클러스터를 다루게 되는데, 이때 어떤 클러스터를 가리키고 있는지 결정하는 것이 `컨텍스트(Context)`다.

    ```bash
    # 현재 연결된 클러스터 확인
    kubectl config current-context
    # 클러스터 변경
    kubectl config use-context [클러스터-이름]
    ```

**⚠️ 주의**: 잘못된 클러스터에 명령을 내리는 사고를 방지하기 위해, 중요한 작업을 하기 전에는 항상 현재 컨텍스트를 확인하는 습관이 중요하다.

</div>

---

## 5. 실습: 쿠버네티스 객체 배포와 검증

이론으로 학습한 내용을 바탕으로 로컬 환경(Docker Desktop + kind)에서 직접 배포와 통신 과정을 실습한다.

<Image
  src="/static/images/kube.png"
  alt="쿠버네티스 설정 활성화"
  className="mx-auto"
  width="900"
  height="600"
/>

Docker Desktop 설정에서 Kubernetes를 활성화하고 약 1분 정도 기다리면 상태가 Running으로 변경된다. 이후 아래 명령어로 클러스터 연결을 확인한다.

```bash
kubectl get nodes
```

### 1) 네임스페이스 생성

리소스를 논리적으로 격리하여 관리하기 위해 별도의 네임스페이스를 생성한다.

```bash
kubectl create namespace [네임스페이스]
```

### 2) Deployment 배포 (YAML 활용)

미리 빌드하여 Docker Hub에 푸시한 이미지를 사용하여 3개의 파드를 유지하는 배포 파일을 작성하고 적용한다.

```yaml
# k8s/deployment.yaml
apiVersion: apps/v1
kind: Deployment # 리소스 종류 정의 (배포 관리자)
metadata:
  name: hello-deployment
  namespace: dev-test
spec:
  replicas: 3 # 항상 유지할 파드(Pod) 개수
  selector:
    matchLabels:
      app: hello-app
  template:
    metadata:
      labels:
        app: hello-app
    spec:
      containers:
        - name: hello-container
          image: parklego/hello-docker:v1
          ports:
            - containerPort: 8080 # 컨테이너가 내부적으로 사용하는 포트
```

### 3) 배포 적용 및 상태 확인

```bash
kubectl apply -f k8s/deployment.yaml # 설정 파일의 내용을 클러스터에 적용
kubectl get pods -n dev-test -o wide # 상세 정보(IP, 노드 등) 포함하여 확인
```

### 4) 내부 상태 확인 및 셀프 힐링 테스트

배포된 파드들이 의도한 대로 동작하고 있는지, 그리고 쿠버네티스의 핵심 기능인 '상태 유지'가 잘 작동하는지 검증한다.

#### 로그 확인 (kubectl logs)

단순 출력을 담당하는 스크립트 기반 컨테이너는 외부 통신 프로토콜(HTTP 등)이 없으므로, 로그를 통해 정상 동작 여부를 확인한다.

```bash
# kubectl logs [파드-이름] -n dev-test
kubectl logs [파드-이름] -n dev-test
```

#### 셀프 힐링 테스트

실행 중인 파드 하나를 강제로 삭제했을 때, Deployment가 설정된 `replicas: 3`을 유지하기 위해 즉시 새로운 파드를 생성하는지 확인한다.

```bash
kubectl delete pod [파드-이름] -n dev-test # 특정 파드를 삭제하여 장애 상황 시뮬레이션
kubectl get pods -n dev-test -w # -w 옵션으로 실시간 상태 변화 감시
```

### 5) 외부 연결 테스트 (Port-forward)

클러스터 외부(로컬 PC)에서 파드 내부로 접근하기 위해 포트 포워딩을 사용한다. 웹 서버가 내장된 `nginx` 이미지를 활용해 브라우저에서 직접 접속을 확인해 본다.

```bash
# nginx 파드 생성
kubectl run nginx-test --image=nginx -n dev-test

# 포트 포워딩 실행 (로컬 8091 -> 파드 80)
kubectl port-forward pod/nginx-test 8091:80 -n dev-test # nginx는 기본적으로 80 포트에서 요청을 대기한다.
```

웹 브라우저 주소창에 localhost:8091 입력 시 Welcome to nginx! 페이지가 나오면 성공!!

---

## 6. 정리

도커 이미지를 만드는 기초부터 쿠버네티스 클러스터 위에서 리소스를 선언하고 유지하는 과정까지 전체적인 흐름을 정리해 보았다. 이번 학습과 실습을 통해 얻은 핵심 포인트는 다음과 같다.

- **인프라의 선언적 관리**: 쿠버네티스는 '어떻게 실행할지'가 아니라 '어떤 상태가 이상적인지'를 YAML로 선언하는 것이 핵심이다. 시스템이 이 상태를 24시간 감시하며 현실로 유지해준다.
- **격리와 공유의 균형**: `Pod`를 통해 컨테이너 간 리소스를 공유하고, `Namespace`를 통해 팀이나 프로젝트 단위로 논리적인 구역을 안전하게 격리할 수 있다.
- **안정성과 접근성**: `Deployment`의 셀프 힐링 기능을 통해 인프라의 신뢰성을 확보하고, `Port-forward`를 활용해 외부에서 내부 서비스에 안전하게 접근하여 검증하는 실무적인 방법을 익혔다.

결국 도커가 '애플리케이션을 담는 표준 규격'을 만들었다면, 쿠버네티스는 그 규격을 '어떻게 효율적이고 안정적으로 운영할 것인가'에 대한 해답을 제시한다. 이번 실습을 통해 로컬 환경에서도 충분히 클러스터의 동작 원리를 체감할 수 있었으며, 이는 향후 `EKS`나 `Helm` 같은 복잡한 도구를 다룰 때 단단한 기초가 될 것이다.
